// config.js — backend URL (update this after starting the server)
// Copy the trycloudflare URL printed by start.sh and paste it here.

export const API_URL = "https://your-tunnel.trycloudflare.com";
